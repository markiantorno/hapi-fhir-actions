ame: "Download and Process Artifacts"
description: "Downloads artifacts for a list of modules and prepares them for further steps."

inputs:
  modules:
    description: "A JSON list of modules to process"
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - name: Iterate through modules and download artifacts
      shell: bash
      run: |
        modules=${{ inputs.modules }}
        echo "Processing modules: $modules"
        
        # Loop through each module using jq to parse the JSON input correctly
        echo "$modules" | jq -c '.[]' | while read -r module; do
          echo "Processing module: $module"
          
          # Create the target directory for each module
          mkdir -p "${GITHUB_WORKSPACE}/$module/target"
          
          # Clean Module Name (basename logic)
          cleaned_name=$(basename "$module")
          echo "Cleaned module name: $cleaned_name"
          
          # Download the artifact for the cleaned name
          echo "Downloading artifact for $cleaned_name..."
          gh api \
            -H "Accept: application/vnd.github.v3+json" \
            "/repos/${{ github.repository }}/actions/artifacts/${cleaned_name}_target/zip" > "${GITHUB_WORKSPACE}/${module}/target/${cleaned_name}_target.zip"
          
          # Unzip the artifact
          unzip "${GITHUB_WORKSPACE}/${module}/target/${cleaned_name}_target.zip" -d "${GITHUB_WORKSPACE}/${module}/target/"
          
          # Debug directory listing after downloading and extracting the artifact
          echo "Listing target directory for $module:"
          ls "${GITHUB_WORKSPACE}/${module}/target/"
        done
    
    # - name: Download Jacoco test coverage result for each module
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: ${{ steps.clean_module_name.outputs.cleaned_name }}_target
    #     path: ${{ github.workspace }}/${{ matrix.module }}/target/
